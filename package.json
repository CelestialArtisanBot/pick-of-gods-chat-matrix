{
  "name": "pick-of-gods-chat-matrix",
  "version": "1.0.0",
  "description": "Pick of Gods AI Chat with Matrix Rain, Cloudflare Workers AI, and image generation",
  "author": "Pick of Gods",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev src/index.ts --local",
    "build": "tsc",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.2025.0"
  }
}
