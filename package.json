{
  "name": "pick-of-gods-chat-matrix",
  "version": "1.0.0",
  "private": true,
  "description": "Pick of Gods AI Chat Platform",
  "scripts": {
    "start": "wrangler pages dev ./public --compatibility-date=2025-09-03",
    "build": "tsc",
    "deploy": "npm run build && wrangler pages deploy ./public --compatibility-date=2025-09-03"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.2"
  }
}
